# D3-8 演示视频脚本（≤ 5 分钟）

**产出编号**：D3-8  
**产出名称**：演示视频脚本  
**所属实验**：实验三 DevOps 流水线与容器化部署  
**班级 / 学号 / 姓名**：计算机23-2 / 230502209 / 潘美儒  

---

## 视频总时长：约 4 分 30 秒

| 时间段 | 画面内容 | 旁白 |
|--------|----------|------|
| 0:00-0:30 | **标题页**：NekoCafé DevOps 流水线演示 / 姓名学号 | "大家好，我是计算机23-2班潘美儒。本视频演示 NekoCafé 猫咪主题餐饮预约平台的 DevOps 流水线与容器化部署。" |
| 0:30-1:00 | **仓库结构概览**：VSCode 打开项目，快速浏览目录树 | "项目采用 Monorepo 结构，包含预约服务和会员服务两个核心微服务。接下来我们看一键启动。" |
| 1:00-1:40 | **本地起栈**：终端执行 `docker compose up -d`，等待服务启动，`curl localhost:8000/health` 验证 | "运行 docker compose up 一行命令即可启动全部服务。可以看到两个服务均健康运行。" |
| 1:40-2:10 | **API 演示**：浏览器打开 Swagger `localhost:8000/docs`，执行一个预约创建请求 | "通过 Swagger 文档可以直接测试 API。这里我们创建一个预约，返回 201 创建成功。" |
| 2:10-2:50 | **CI 流水线**：浏览器切换到 GitHub Actions 页面，展示 CI 运行过程，各 Stage 全部绿色 | "每次 Push 或 PR 都会自动触发 CI 流水线，包含 Lint 检查、单元测试、Trivy 安全扫描、镜像构建推送四个阶段。" |
| 2:50-3:20 | **CD 蓝绿部署**：展示 CD 流水线运行，Switching traffic to Green 步骤 | "CD 采用蓝绿部署策略。先部署新版本 Green，健康检查通过后切换流量，旧版本 Blue 保留 5 分钟作为回滚窗口。" |
| 3:20-3:50 | **Grafana 监控**：浏览器打开 Grafana Dashboard，展示 QPS、P99 延迟、错误率、资源使用 4 个面板 | "可观测性方面，集成了 OpenTelemetry + Prometheus + Grafana。Dashboard 展示 QPS、P99 延迟、错误率和资源使用四类指标。" |
| 3:50-4:20 | **告警与回滚**：展示 Prometheus 告警规则，演示回滚脚本 | "配置了 5 条告警规则。当 P95 延迟超过 500ms 或错误率超过 1% 时自动触发回滚。一键执行 rollback.sh 即可回退到上一版本。" |
| 4:20-4:30 | **总结**：仓库地址 + 感谢观看 | "以上是实验三的全部演示内容。完整的代码和文档已上传至 GitHub 仓库。谢谢观看！" |

---

## 录制准备清单

- [ ] 确保 `docker compose up -d` 可正常启动
- [ ] 确保 Swagger 页面可访问 `http://localhost:8000/docs`
- [ ] 提前跑过一次 CI/CD，确保 GitHub Actions 有历史运行记录
- [ ] Grafana Dashboard 已配置好并有数据
- [ ] 屏幕录制工具：OBS / macOS QuickTime / Windows Xbox Game Bar
- [ ] 确保终端字体足够大，清晰可见
