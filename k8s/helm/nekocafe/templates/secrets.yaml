apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secrets
  labels:
    release: {{ .Release.Name }}
type: Opaque
stringData:
  jwt-secret: {{ .Values.member.env.JWT_SECRET | default "nekocafe-default-jwt-secret-please-change" | quote }}
